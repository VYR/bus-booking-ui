import { HasEntitlementDirective } from './has-entitlement.directive';

describe('HasEntitlementDirective', () => {
  it('should create an instance', () => {
    const directive = new HasEntitlementDirective();
    expect(directive).toBeTruthy();
  });
});
